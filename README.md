# Carepack (Vite + React + i18n)

## Run locally
```bash
npm i
npm run dev
```

## Build
```bash
npm run build
npm run preview
```

## Notes
- Languages: ET / RU / EN (react-i18next)
- Replace icons in `public/icons/*.png` with your generated illustrations (same filenames).
